

    <div class="pagetitle">
      <h1>departamentos</h1>
      <br/>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><%= link_to 'Inicio', "/" %></li>
          <li class="breadcrumb-item active">departamentos</li>
        </ol>
      </nav>
    </div>

    <div class="row">
        <div class="col-6"></div>
        <% if acceso_a_accion_de_modulo?(current_usuario, "departamentos", "crear") %>
        <div class="col-6" style="text-align: right;">
            <%= link_to 'Agregar departamento', new_departamento_path, {class: "btn btn-primary", style: "margin-top:-100px"} %>
        </div>
        <%end%>
    </div>
<section class="section">
        <div class="row">
          <div class="col-lg-12">
  
            <div class="card">
              <div class="card-body">
                <h5 class="card-title text-center" >Administrar departamentos</h5>
  
                <!-- Table with stripped rows -->
                <table class="table datatable">
                  <thead>
                    <tr>
                      <th scope="col">#</th>
                      <th scope="col">Nombre</th>
                      <th scope="col">Acciones</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% @departamentos.each do |departamento| %>
                        <tr>
                            <th scope="row"><%= departamento.id %></th>
                            <td><%= departamento.nombre %></td>
                            <td>
                                <%= link_to '', edit_departamento_path(departamento), {class: "bx bx-pencil", style: "color:blue;"} if acceso_a_accion_de_modulo?(current_usuario, "departamentos", "actualizar") %>
                                <%= link_to '', departamento, {method: :delete, class: "bx bx-trash", style: "color:red;", data: {confirm: "Estas seguro de eliminar este departamento?"}} if acceso_a_accion_de_modulo?(current_usuario, "departamentos", "eliminar") %>
                            </td>
                        </tr>
                    <% end %>
                  </tbody>
                  
                </table>
                <!-- End Table with stripped rows -->
  
              </div>
            </div>
  
          </div>
        </div>
      </section>

    <script>
        $( document ).ready(function() {
            $("#departamentos").removeClass("collapsed");
        });
    </script>